version: '3'

services:
 
  mysql_db:
    image: mysql
    container_name: mysql
    restart: always
    ports:
      - '3306:3306'
    environment:
      - MYSQL_ROOT_PASSWORD=Th3gr3at
  
       
  
